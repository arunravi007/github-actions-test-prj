name: Shell commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo Hello World
        run: echo "Hello World!"
      - name: Multiline script
        run: |
          node -v 
          npm -v